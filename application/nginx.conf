server {
    listen 80;

    # Next.js
    location / {
        proxy_pass http://frontend:3000;
        proxy_http_version 1.1;
    }

    # Backend
    location /api/ {
        proxy_pass http://backend:8000/;
        proxy_http_version 1.1;
    }
}
