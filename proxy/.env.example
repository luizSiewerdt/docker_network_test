# URL do backend real que o proxy vai encaminhar as chamadas de /users.
BACKEND_URL=http://CHANGE_ME:8000

# URL do banco de dados externo, usada pelos endpoints em /db.
DATABASE_URL=postgresql://user:password@CHANGE_ME:5432/dbname
